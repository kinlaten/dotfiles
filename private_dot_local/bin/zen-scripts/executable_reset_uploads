#!/usr/bin/zsh
UPLOADS_DIR="/home/zen/.zen_scripts/uploads"

# Delete and recreate the directory
if [ -d "$UPLOADS_DIR" ]; then
    rm -rf "$UPLOADS_DIR"
fi
mkdir -p "$UPLOADS_DIR"

echo "Uploads directory reset on $(date)" >> /var/log/uploads_reset.log

